import Foundation

enum CornerRadius {
    /// 10pt:small badges (icon badge)
    static let sm: CGFloat = 10

    /// 12pt:inner elements (weight cards)
    static let md: CGFloat = 12

    /// 16pt:cards, inputs, selection pills
    static let lg: CGFloat = 16

    /// 20pt:profile headers, date cards
    static let xl: CGFloat = 20
}
